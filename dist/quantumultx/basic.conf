# Generated by LoneRules. DO NOT EDIT; run python -m src.build.
# Import your own servers, then switch the Proxy group from direct to proxy.
# Before that switch, this starter configuration routes traffic directly.
# This profile embeds a rule snapshot; re-import a newer release to update it.

[general]

[dns]
# Use the DNS servers supplied by the current network.

[policy]
static = Proxy, direct, proxy
static = AI, Proxy, direct
static = Apple, direct, Proxy
static = Streaming, Proxy, direct
static = Social, Proxy, direct
static = China, direct, Proxy
static = Global, Proxy, direct

[server_remote]
# Add your personal Quantumult X server subscription in the app.

[server_local]
# Alternatively, add your own server in the app.

[filter_remote]
# Rules are embedded below; no remote rule download is required.

[filter_local]
host-suffix,openai.com,AI
host-suffix,chatgpt.com,AI
host-suffix,oaistatic.com,AI
host-suffix,oaiusercontent.com,AI
host-suffix,apple.com,Apple
host-suffix,icloud.com,Apple
host-suffix,mzstatic.com,Apple
host-suffix,netflix.com,Streaming
host-suffix,nflxvideo.net,Streaming
host-suffix,spotify.com,Streaming
host-suffix,discord.com,Social
host-suffix,discordapp.com,Social
host-suffix,twitter.com,Social
host-suffix,x.com,Social
host-suffix,baidu.com,China
host-suffix,qq.com,China
host-suffix,taobao.com,China
host-suffix,github.com,Global
host-suffix,githubusercontent.com,Global
host-suffix,wikipedia.org,Global
# Local-network fallbacks apply after the maintained service rules.
ip-cidr,127.0.0.0/8,direct
ip-cidr,10.0.0.0/8,direct
ip-cidr,172.16.0.0/12,direct
ip-cidr,192.168.0.0/16,direct
ip-cidr,169.254.0.0/16,direct
ip6-cidr,::1/128,direct
ip6-cidr,fc00::/7,direct
ip6-cidr,fe80::/10,direct
geoip,cn,China
final,Global

[rewrite_remote]

[rewrite_local]

[task_local]

[mitm]
